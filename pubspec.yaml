name: projeto_valid
description: A new Flutter project
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  device_preview: ^1.1.0
  flutter:
    sdk: flutter

  url_launcher: ^6.0.12 
  cupertino_icons: ^1.0.2
  image_picker: ^0.8.4+4
  syncfusion_flutter_xlsio: 23.1.42
  spreadsheet_decoder: ^2.0.0
  http: ^0.13.3
  firebase_core: ^2.23.0
  cloud_firestore: ^4.13.2
  firebase_auth: ^4.14.1
  firebase_database: ^10.3.3
  provider: ^6.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/
    - assets/example.xlsx
